%
%=====================Code Listing Layout Matlab=============================
\definecolor{codegreen}{RGB}{28,172,0} % color values Red, Green, Blue
\definecolor{codedarkgreen}{RGB}{94,153,85}
\definecolor{codelilas}{RGB}{170,55,241}
\definecolor{codeorange}{RGB}{155, 55, 31}
\definecolor{codegrey}{rgb}{0.6,0.6,0.6}
\lstdefinestyle{mypython}{
    language=Python,
    tabsize=1,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    basicstyle=\ttfamily\footnotesize,
    inputencoding=utf8,
    breaklines=true,
    stepnumber=1, % line numbering steps
    % morekeywords={ones,height,width,datetime}, % additional keywords to highlight
    keywordstyle=\color{blue},
    frame=none,
    identifierstyle=\color{black},
    stringstyle=\color{codeorange},
    commentstyle=\color{codegreen},
    showstringspaces=false, %without this there will be a symbol in the places where there is a space
    numbers=left,
    numberstyle={\tiny \color{black}}, % size of the numbers
    numbersep=-6pt % this defines how far the numbers are from the text
}
\lstdefinestyle{mypython-apx}{
    language=Python,
    tabsize=2,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    basicstyle=\ttfamily\footnotesize,
    inputencoding=utf8,
    breaklines=true,
    stepnumber=1, % line numbering steps
    % morekeywords={ones,height,width,datetime}, % additional keywords to highlight
    keywordstyle=\color{blue},
    frame=none,
    identifierstyle=\color{black},
    stringstyle=\color{codeorange},
    commentstyle=\color{codegreen},
    showstringspaces=false, %without this there will be a symbol in the places where there is a space
    numbers=left,
    numberstyle={\tiny \color{black}}, % size of the numbers
    numbersep=5pt % this defines how far the numbers are from the text
}
\lstdefinestyle{mybash}{
    language=sh,
    tabsize=1,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    basicstyle=\ttfamily\footnotesize,
    inputencoding=utf8,
    breaklines=true,
    stepnumber=1, % line numbering steps
    morekeywords={lsusb,grep,sudo,echo,conda,pip,chmod,tee,apt,groups}, % additional keywords to highlight
    keywordstyle=\color{blue},
    frame=none,
    identifierstyle=\color{black},
    stringstyle=\color{codeorange},
    commentstyle=\color{codegreen},
    showstringspaces=false, %without this there will be a symbol in the places where there is a space
    numbers=left,
    numberstyle={\tiny \color{black}}, % size of the numbers
    numbersep=-6pt % this defines how far the numbers are from the text
}
%==============================================================================